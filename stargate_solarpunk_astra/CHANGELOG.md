# Changelog

All notable changes to The Loamwake Commons are documented here, following Keep a Changelog conventions.

## [1.0.0] - 2026-09-04

**Built by:** gpt-6-astra (high reasoning).

Attribution was checked against the current session’s recorded runtime metadata before this file was written. It reported `gpt-6-astra` with effort `high`, matching the requested attribution. No model reroute was observed in that record.

### Added

- An original community soil-restoration identity with eight drawn address signs, four independently growing root unions, and converging lock tones.
- Separate ready and activation states, a 2.1-second buildup, an 0.8-second contact breakthrough, and sustained visual and audio exchange.
- Two sequential remembered routes, an initially released resting-soil hold, and immediate disengage from either view.
- A nine-entry searchable ledger with typed relations, generated backlinks, bookmarks, place-to-route actions, and explicitly declared localStorage persistence.
- A responsive folio-and-instrument composition, visible keyboard focus, live captions, and reduced-motion support.
- A dependency-free local static server and an 800 × 450 gallery preview.

### Verification

- Direct Puppeteer checks in Chrome recorded 29 passing observations and 50 mouse clicks at hit-tested rendered positions, plus a complete keyboard-operated exchange. Layout defects found during review were corrected and the affected checks rerun. No uncaught browser errors were recorded.
- At 1920 × 1080, the stage transform was `matrix(1.2, 0, 0, 1.2, 0, 0)`; at 3840 × 2160 it was `matrix(2.4, 0, 0, 2.4, 0, 0)`. Both footers stayed within the viewport. At 390 × 844, document width was exactly 390 px and opening/disengaging succeeded.
- A manual root was observed at 47.9% progress with zero joined arcs. Four manual signs and the remembered route each remained `ready` after 3.2 seconds without activation input. Screenshots: `03-manual-root-growing.png`, `04-manual-ready.png`, `09-preset-first-in-motion.png`, and `10-preset-middle-in-motion.png`.
- Activation screenshots show buildup at approximately 650 ms (`06-buildup.png`), breakthrough observed at 2,180 ms (`07-breakthrough.png`), and sustained active captured by 3,323 ms (`08-sustained-active.png`). Measured output gains were approximately 0.055, 0.140, and 0.033 respectively. These are browser audio-graph measurements, not an operator listening test.
- Two full manual dial/open/disengage cycles ended idle with an empty address, zero joined arcs, zero oscillator voices, and zero output gain. The desktop disengage button was hit-tested at approximately (1678.8, 968.4).
- Disengage at approximately 564 ms into buildup remained idle after a further 8.25 seconds: no automatic sequence, no pending lock, unchanged reset root-path offsets, zero oscillator voices, and zero output gain. Screenshots: `11-cancelled-buildup.png` and `12-still-idle-after-eight-seconds.png`. A separate interrupted preset also stayed idle.
- The resting-soil hold began released and blocked an attempted opening when engaged, with a highlighted live message and nonzero warning gain (`05-hold-blocked.png`).
- All nine ledger entries resolved their 27 typed links and 27 generated backlinks. Bookmark filtering returned one saved entry after bookmarking; all three declared persistence keys restored the intended content while the exchange restarted idle and the hold released. Malformed and unavailable storage did not prevent dialing.
- Keyboard input completed a four-sign route, opening, and disengage; focus was visible with a solid 3 px outline. Reduced motion retained buildup and live text while sustained decorative animation computed to `none` (`14-reduced-motion-buildup.png`). Both views retained a reachable disengage control, including mobile.
- Visually reviewed the stage, preset, archive, and mobile screenshots. The corrected ring labels and desktop folio fit their bounds. The sole committed image, `preview.png`, was checked at 800 × 450.
- Source review found no private paths, identifying user data, credentials, unrelated build references, or external content dependencies. Evidence screenshots, browser profiles, and detailed run logs remain in the ignored `test/` directory.
- Operator hands-on testing remains the real final check. Automated input dispatch is not equivalent to that check.
